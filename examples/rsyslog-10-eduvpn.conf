# /etc/rsyslog.d/10-eduvpn.conf
# Route correlator events (syslog ident "eduvpn-logger", facility local0)
# to a dedicated file. Adjust the programname if you set EDUVPN_SYSLOG_IDENT.
if $programname == 'eduvpn-logger' then /var/log/eduvpn/eduvpn-syslog.log
& stop
